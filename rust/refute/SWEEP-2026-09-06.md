# Sweep r5 — seven lenses, manual triage (2026-09-06 evening)

Workflow `kakeya-verify-and-sweep` (run `wf_197142db-b96`, 16:20–17:31 EDT): seven
independent Opus lenses over the on-disk artifacts, nothing rebuilt in place.
**The adjudication stage (18 refuters) and the completeness critic did not run** —
the account's session limit tripped after the seven lenses returned. This file is
the manual triage that replaced it: every load-bearing claim below was re-measured
from the CLI before its verdict was written. Ground rules held: `git status` shows
nothing under `crates/`, `src/`, `results.json`, `RESULTS-RUST.json`,
`PROGRESS.md`; the only modified files were written by the two long jobs
themselves. No process was signalled by any lens (several noted pid 12158
vanishing — that was the 8-cycle run finishing normally at 20:46Z).

| lens | file | findings | verdict on the claim |
|---|---|---|---|
| hitpaths | `search/r5-hitpaths/FINDINGS.md` | 8 | not refuted |
| external | `external/r5/FINDINGS.md` | 7 | "refuted" on driver-unreachable inputs only |
| record | `record/r5/FINDINGS.md` | 14 | REFUTED (the record, not the code) |
| kernel | `kernel/r5/FINDINGS.md` | 5 | not refuted |
| science | `science/r5/FINDINGS.md` | 10 | REFUTED (a closure claim was scoped narrower than stated) |
| perf | `../improve/perf/REPORT.md` | 8 | improvement, 4.7x prototype |
| robustness | `../improve/robustness/REPORT.md` | 3 | improvement, both OPEN limits closed in a copy |

## A. Confirmed by my own reproduction (record corrected)

1. **cycles8 never searches t = 3, and t = 3 was inside the target** (science F1).
   `cycles8.py:62` `for t in (0, 1, 2)`; `cycles8.rs:195` `(0..=2)`. At POOL6 / 67/40,
   den = 5, budget = int(67/40·5) − 8 = 0, r = 0, score 8/5 = 1.600 ≤ 1.675. So every
   "cycles8 POOL6 closed" sentence in the record was silently scoped to t ≤ 2.
   Reproduced `t3_check.py` (pure Python): patterns 2,3 → 2,592 graphs / 87,600 pairs /
   0 hits (13.6 s); patterns 1,4 → 7,992 / 236,400 / 0 (33.2 s). Pattern 0 run in pure Python
   to replace the lens's component-split argument with a computation: 46,656 graphs,
   1,368,000 pairs, 0 hits, 567 s (`science/r5/t3_idx0_purepy.txt`). **t = 3 closed by
   computation over all five patterns, 1,692,000 pairs, 0 hits. cycles8 POOL6 is closed at every t.**
2. **The 8-cycle cross-check completed**: 81,660 tested, 0 hits, exit 0, 6,214 s at 10
   threads (13.1 pairs/s; the "~5 h" estimate was 3x high). Added to RESULTS-RUST.json as run 6.
3. **The pure-Python POOL3 oracle completed** (17:38 EDT), byte-identical to Rust at 1
   and 12 threads. Kernel-disclosure gap on the cycles8 row closed.
4. **Record R1 — two more coverage rows had the PyO3 kernel on the Python side**
   (g2-tall 2x4: `py_g2tall_2x4_rskernel.txt`; stacked POOL4 max_t=1). My own
   disclosure paragraph from the afternoon said "unlike every other row". Wrong; fixed.
5. **Record R2 — the stacked POOL4 max_t=1 Python artifact is 0 bytes**
   (`py_stacked_p4_t1_rskernel.txt`, `py_stacked_p4_t0.txt`). The substance is
   supported by the max_t=2 comparison against the pure-Python `logs/stacked.log`
   (md5-equal, r3); the max_t=1 row as a separate comparison is unsupported. Map fixed.
6. **Record R3 — two coverage rows have no artifact anywhere** (`--d 6`: sweep config
   c12's Python output is 0 bytes; `--d 2x2x2`: configs s17b/s18/s19 never run). Map fixed.
7. **Record R4/R5 — "30/30" and "37 configurations" are not on-disk counts.** Measured:
   29 configs with artifacts, 28 non-empty Python outputs; the lens's recount 25 PASS /
   1 FAIL (s09 = known Python ZeroDivisionError). COMMANDS.md annotated.
8. **Record R6 — "135 trap cases" double-counts.** `traps.json` = 119 entries and already
   contains the 17 `v`-prefixed VERIFIED.md cases (a 36, d 18, f 18, v 17, e 16, b 9, c 5).
   Mine again; fixed in PROGRESS.md; handoff's "136" noted.
9. **Record R7/R8** — PORT-TRAPS: 17 `v` entries not 16; its "no `_force_py` split, no
   `KAKEYA_PURE_PY`" sentence is now false in the direction that reintroduces kernel
   substitution. Errata appended to PORT-TRAPS.md.
10. **Record R9** — PREDICTIONS §2.2 pattern order wrong (its own §3 pairs list is
    right). Errata appended to PREDICTIONS.md.
11. **Record R11** — PREDICTIONS §6 ">10x wrong" is mis-cited and unsupported; measured
    bound ≤ ~41 CPU-h for 2x5 (prefix rate) vs the science lens's ~12 CPU-h (later
    pairs). **The two lenses disagreed 3.5x on the 2x5 cost and this triage split the difference ("12–41 CPU-h") — that was wrong. Adjudication FINDING-10 REFUTED the lens: the recorded 2x5 run already burned 16.0 thread-hours (run_targets.sh T=8 x 7,207 s), which excludes the low estimate outright; a complete 2x5 is ~25–40 CPU-h and PREDICTIONS' ">10x" stands. The first erratum (which withdrew a true claim) has been rewritten.** See sec G.
12. **Record R10/R12/R13/R14** — Lemma C misattribution (PROGRESS fixed); RESULTS-RUST
    C6 note stale (fixed); 8-cycle result not in record (fixed); COMMANDS §9 "75
    tests" (annotated, 78).
13. **External — `scan --tlimit 0`**: Python `if tlimit and` (search.py:209) = no limit;
    Rust = 0 s limit. In-contract CLI input no driver passes. OPEN in PROGRESS.md.
14. **Kernel — the shipped `.so` predates the last kakeya-core edit by 25 h**, sha
    pinned nowhere; a fresh post-edit build shows no drift. Sha now pinned in PROGRESS.md.

## B. Confirmed as scope notes (not defects in any recorded run)

* External: `force` reads `rows` by iteration where Python subscripts — dict / list-subclass
  rows give a silent wrong value. No driver passes such rows. The lens itself says to
  downgrade its `refuted=true` under the driver-input reading of "in-contract". Agreed.
* Hitpaths: `g2-tall --rows 1 --max-t 2` — Python ZeroDivisionError (den = 0), Rust clean
  RESULT. Out of contract (rows ∈ {4,5,6}); s09's class.
* Hitpaths: rzero's hit line is unreachable for every input (edge-row column-sum
  invariant, corroborated over 84,718 graphs) — consistent with the workbench, which
  calls rzero "a direct check of P7". stacked's hit block is empty for POOL3–6 by pool
  monotonicity; only POOL8 is open. Hence no configuration fires four of the five
  drivers' hit paths, and the lens verified them at the formatter level instead: CPython
  executing the drivers' own `print` statements (extracted by `ast`, never retyped) vs
  the Rust formatters — 325 lines, 0 mismatches, negative control catches 11/11.
  This is the strongest check available for those paths and it is now on the map.

## C. New positive evidence banked

* 557,055-case kernel sweep vs pure Python, 0 mismatches (entries to ±2^63, moduli to
  2^32±1) — ~10x difftest.py. (external)
* `scan --seed` verified at 9 seeds spanning 2^32 and up to 2^64−1, 9/9, on runs with
  63–78 HITOBJ each — previously `--seed` had no oracle at any value but 11. (external)
* 15 new end-to-end driver comparisons, 14 pure-Python, all identical. (hitpaths)
* Every existing kernel refuter suite re-run against the current `.so` and the pure
  fallback: no failures; 30+ new hand cases and a 20k fresh-seed fuzz added and passed. (kernel)
* Five antigravity-cli engine/determinism claims tested to destruction and refuted. (external)

## D. Improvements — built, gated, NOT adopted (adoption = rebuild the audited artifacts + full regate)

* **perf** (`improve/perf/`): prototype `force` **4.7x** faster, byte-identical stdout on
  three driver runs; 27,951-case differential vs the PURE-PYTHON force (five moduli,
  exceptions included), 0 mismatches in every kernel mode. Levers: Mersenne reduction for
  p = 2^31−1 (2.55x), memoised pivot inverse (1.7–1.9x). Lever C (carry RREF across
  warm-start) unspent, est. 2–3x more. Corrects the record's O(|U|·rk·w) estimate for
  step 4 (already O(|U|·2·w)).
* **robustness** (`improve/robustness/`): streaming chunk enumeration (+58 −14 in
  engine.rs). `check` identical at 1/12 threads; r3 fixtures 15/15 byte-identical;
  78/78 + clippy clean; the pool-3 abort now returns a RESULT in 2.03 s at 1.26 MB RSS;
  `--tlimit 5` honoured at 5.01–5.02 s on every index space tried. Residual overshoot is
  per-item DFS cost, present identically in Python. g2_tall proposal: keep the extra
  keys, add `--python-compat`.

## E. Research-level caveats for Alexander (science lens Q2–Q5; not verified by me beyond spot checks)

* **Every negative in the campaign is conditional on the matching-suffix reading of
  Operation 1** — the workbench argues only hit-transfer, and the readings do separate
  (456/8,320 configs on d=[2,2]). **DISPOSED on adjudication (FINDING-04):** the smallest
  separating object is permissive-valid at score 5/3 on FOUR vertices, below the Epoch
  target and below gamma — so if permissive were the intended reading the problem would be
  solved by a 2x2 box and Katz–Tao would not be a record. The campaign's reading is the only
  one consistent with the problem being open. One sentence for the workbench; no re-runs.
* **The 3/2 barrier (Katz 2006) is load-bearing for the framing and is not in `papers/`**
  (checked: `papers/` has katz-tao-0010069 and kt.pdf, no Katz 2006).
* **"Complete" sweeps are complete in the labellings and a single point in the slope
  moduli**; no run varies a modulus at n > 4 (`moduli_check.py`).
* **g2_tall pins f1 = (1,0)**, and the usable Goal-2 label symmetry group has order 2
  (measured), so even a complete 2x5/2x6 leaves half the d = [2,R] three-slope space
  unsearched.
* **2x6 to completion ≈ 1,030 CPU-h** (infeasible); 2x5 ≈ 12–41 CPU-h (the two lenses
  disagree; see A.11). The "6.3 GB RSS" concern is stale — the row_pool leak is fixed.
* **cycles8 covers 6.95% of the 2x2x2 POOL6 label space**; "cycles8 POOL6 closed" must
  not be read as "n = 8 closed". `--deg` is vacuous for every value but 2.
* **Next computations by information per CPU-hour** (science §4, commands in its file):
  (1) the first complete Goal-1 sweep at n = 8, ~6.3 CPU-h; (2) complete d = [2,4] for
  Goal 2 over ALL f1, ~1–2 CPU-h; (3) g2_tall 2x5 to completion, 12–41 CPU-h.

## F. Still open after this sweep

* Adjudication never ran; this file is a single-reviewer triage. The workflow can be
  resumed after the limit reset (`resumeFromRunId: wf_197142db-b96`, the seven lens
  results are cached) if a second opinion on any item above is wanted.
* The two UNSUPPORTED coverage rows (`--d 6`, `--d 2x2x2`) need a real re-run.
* `--tlimit 0` semantics; `.so` rebuild + sha pin; adoption decision on the two
  improvements; the research caveats in E.
* Sol's audit and r2-vs-r3 ruling (wire polled 18:10 EDT: empty).

## G. Adjudication (second opinions, 10 refuters) and critic — 2026-09-06 22:45–23:25 EDT

Resumed from `wf_197142db-b96` after the session-limit reset; the seven lenses replayed
from cache; one independent Opus refuter per selected finding (cut from 2–3 each at
Alexander's question — the findings were already hand-verified), then a critic.
Evidence: `refute/adjudicate/r5/` (FINDING-*.md, CRITIC-COMPLETENESS.md, scripts, probes).

| finding | verdict | what the refuter added |
|---|---|---|
| science: cycles8 never searches t = 3 | **confirmed**; reclass divergence → coverage-gap (no Python/Rust divergence — both hardcode the same loop) | proved t3_check's hit test IS the driver's (lemma_check: 600 cases both branches); P4 prune is exact at r = 0; force-free recount of all five patterns matches (1,692,000 pairs); **the residual is target-specific** (at --target 2, t = 4 becomes feasible); my 13.6 s/33.2 s "pure Python" timings were PyO3-kernel (fixed) |
| hitpaths: rzero hit line unreachable | **confirmed** (severity → minor) | proof holds line by line; T0 = ∅ on round 1 is the load-bearing step; transfers to Rust |
| hitpaths: stacked hit block empty POOL3–6 | **confirmed** and strengthened | empty at EVERY max_t: t ≥ 4 budget-infeasible (m ≥ 8), t = 3 forces m = 8, r = 0, 0/56 3-subsets force; only POOL8 at t ≤ 2 is open → run started |
| science: matching-suffix conditional | **confirmed** (→ minor) and **disposed** | readings separate (456/8,320 on d=[2,2]); smallest separating object scores 5/3 on four vertices ⇒ permissive cannot be the intended reading |
| perf: 4.7× prototype | **confirmed** (critical → major) | independent 7,990-case corpus at a fresh seed incl. composite p = 91, 0 mismatches all modes; REPORT §8 provenance sentence was false (fixed); rzero not rerouted; recommend levers A + C' + D only |
| robustness: streaming --tlimit | **confirmed** | rebuilt from a different absolute path: `engine.rs.orig` → the AUDITED sha, shipped `engine.rs` → `0a152c93…` — diff provably necessary and sufficient; adopt FIRST |
| record R1: two more PyO3 rows | **confirmed** (critical → major) | the g2-tall 2x4 row cannot be rescued — the only pure-Python-era 2x4 run is `logs/g2_tall.log`, incomplete at 703 s |
| record R2: stacked max_t=1 0-byte | **confirmed** (→ major); correction was incomplete | PROGRESS prose still asserted the old claim (now disclosed); md5-equality is over the CONCATENATION of both Rust logs (fixed); max_t degeneracy measured |
| record R3: two artifact-less rows | **confirmed** (major) | spacing-agnostic re-grep finds the same five Aug-24 Python prefixes and nothing else; sampled fills started |
| record R11: 2x5 ">10x" unsupported | **REFUTED** (→ minor typo) | the lens's "≤2.5x" compared wall-on-8-threads to a sequential estimate; the recorded run's 16.0 thread-hours excludes the low figure; ">10x" stands; my erratum reversed |

**Critic (CRITIC-COMPLETENESS.md), ranked most-misleading first, and what was done:**
1. g2_tall negatives cover t ≤ 1 only; t = 2, 3, 4 inside the 11/6 cap never searched — the cycles8 gap one driver over. → scoped in PROGRESS.md; **2x4 run at `--max-t 4` and `--max-t 6`: both complete, 0 hits, 139,050 pairs (vs 31,718 at t ≤ 1) — closed at every t** (feasible-t table: t = 5 has 19 tuples the critic's list missed, t ≥ 6 none; `adjudicate/r5/g2t4_feasible_t.txt`); 2x5/2x6 stay t ≤ 1.
2. g2_tall RESULT hardcodes `"max_t": 1` both sides → known limit recorded; trust log headers.
3. README gate 5 lacked `KAKEYA_PURE_PY=1` (could not pass) → fixed.
4. PROGRESS prose vs map contradiction on the stacked row → disclosed; concatenation fact fixed.
5. RESULTS-RUST notes[0]/[4] unqualified → both scoped (t ≤ 2 modulo Lemma C + P6; analytic half; 6.95%; target-specific; POOL8 unrun).
6. 13.6 s/33.2 s mislabelled pure-Python → fixed with asserted re-runs (23.9 s / 62.6 s).
7. PREDICTIONS §7 bullet 2 withdrew a true claim → rewritten, ">10x" kept.
8. stacked POOL8 never run → running. cycles8 POOL8 and modulus variation at n > 4 → separate tasks.
9. Permissive reading → disposed (above), sentence lifted into PROGRESS.md.
10. `--d 6` and `--d 2x2x2` rows → sampled fills (limit 2000, seed 11, pure Python vs Rust) running; g2-tall 2x4 pure-Python oracle (~5.5 CPU-h) queued, not started.
11. COMMANDS.md heading and §9 → edited in place with dated markers.
12. stacked t ≥ 3 argument and permissive disposal lived only in untracked adjudicate files → lifted into PROGRESS.md; `adjudicate/r5/` committed.
13. perf REPORT §8 provenance → corrected; scope notes appended.
14. `--tlimit 0` → folded into the robustness adoption recipe.
Ground rules: `git status` on `crates/` and `src/` clean; no process signalled by any agent; one stray `src/__pycache__/*.pyc` (gitignored).
